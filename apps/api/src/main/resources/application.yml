spring:
  application:
    name: kubiku-api
  
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: ${KEYCLOAK_ISSUER_URI:http://localhost:8082/realms/kubiku}
          jwk-set-uri: ${KEYCLOAK_JWK_SET_URI:http://localhost:8082/realms/kubiku/protocol/openid-connect/certs}
  
  datasource:
    url: ${DATABASE_URL:jdbc:postgresql://localhost:5432/kubiku}
    username: ${DATABASE_USERNAME:kubiku}
    password: ${DATABASE_PASSWORD:password}
    driver-class-name: ${DATABASE_DRIVER:org.postgresql.Driver}

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: ${HIBERNATE_DIALECT:org.hibernate.dialect.PostgreSQLDialect}

  flyway:
    enabled: true
    baseline-on-migrate: true
    locations: classpath:db/migration

server:
  port: 8080

springdoc:
  swagger-ui:
    path: /swagger-ui.html
  api-docs:
    path: /v3/api-docs

logging:
  level:
    root: INFO
    org.flywaydb: INFO
    com.kubiku.api: DEBUG
